using UnityEngine;

public class PilotComponent : MonoBehaviour
{
    public virtual void OnProjectileHit(Projectile projectile, DamageController damageController, SkillData data)
    {
        
    }
}